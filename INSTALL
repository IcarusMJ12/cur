1. Get python 3.5+
2. `sudo pip install termcolor`
3. Optionally edit setup.cfg to change the path where the executable scripts will be installed to.
4. Run `sudo python setup.py install`
5. You should now be able to invoke cur.py from the commandline.  It does take the --help flag.
